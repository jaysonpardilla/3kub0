{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'cd core && python manage.py migrate && daphne -b 0.0.0.0 -p $PORT core.asgi:application'"

  }
}
